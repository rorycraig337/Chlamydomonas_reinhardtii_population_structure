##extract a species-wide VCF for all genetically unique field isolates of chlamy, excluding clones and redundant lab isolates

#extract raw SNPs and INDELs
bash select_variants.species_wide.sh
gzip *vcf
